/// @description Insert description here
// You can write your code in this editor

if (!GameManager.gamePaused)
{
	state();
	if (place_meeting(x,y,obj_Pit) && hsp = 0 && vsp = 0) currentHP = 0;
	if (currentHP <= 0) instance_destroy();
}